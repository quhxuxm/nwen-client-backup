import {Injectable} from '@angular/core';
import {ExecutorService} from './executor.service';

@Injectable({
  providedIn: 'root'
})
export class SearchArticleService {
  constructor(private _executorService: ExecutorService) {
  }

  searchRecentCreatedArticle():
}
